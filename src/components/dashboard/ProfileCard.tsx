/**
 * ProfileCard - Re-export from modular structure
 *
 * Note: keep this entrypoint exporting components only (Fast Refresh).
 */
import ProfileCard from "./ProfileCard/ProfileCard";

export default ProfileCard;
export { ProfileCard };
