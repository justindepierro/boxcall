// Stub for ProfessionalPixiEngine to resolve import errors
export type ProfessionalPixiEngine = {
	routesLayer?: any;
	app?: { canvas?: any };
	screenToWorld: (x: number, y: number) => { x: number; y: number };
};
