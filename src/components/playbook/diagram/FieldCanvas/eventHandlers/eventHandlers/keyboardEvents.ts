import type { KeyboardEvent } from "react";
import type { FieldCanvasContextType } from "../FieldCanvasContext";

export function handleKeyDown(
  evt: KeyboardEvent,
  _ctx: FieldCanvasContextType
) {
  // Example: handle undo/redo, delete, arrow keys
}
