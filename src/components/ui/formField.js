export function createFormField({}) {
  const el = document.createElement('div');
  el.className = 'border rounded p-4 text-center text-gray-700';
  el.textContent = 'FormField component placeholder';
  return el;
}
