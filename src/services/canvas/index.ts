/**
 * Canvas Abstraction Layer
 *
 * Clean, unified API for diagram rendering across different canvas libraries
 */

export { createDiagramCanvas } from './DiagramCanvas';
export type { IDiagramCanvas, CanvasConfig } from './DiagramCanvas';